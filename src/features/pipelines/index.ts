import { registerFeature } from '../../core/router/index.js'

registerFeature({
  id: 'pipelines',
  name: 'Pipelines',
  routes: [],
  navigationItems: [{ key: 'p', label: 'Pipelines', route: 'pipelines' }],
})
