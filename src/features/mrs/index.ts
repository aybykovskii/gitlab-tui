import { registerFeature } from '../../core/router/index.js'

registerFeature({
  id: 'mrs',
  name: 'Merge Requests',
  routes: [],
  navigationItems: [{ key: 'm', label: 'Merge Requests', route: 'mrs' }],
})
